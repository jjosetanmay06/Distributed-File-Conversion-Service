# Distributed-File-Conversion-Service
This project implements a distributed image conversion system where a client sends an image and desired format to a master server, which forwards the task to a worker node for processing. The worker converts the image (e.g., PNG ↔ JPG) and returns the result through the master server back to the client.
